server:
  port: 8001

spring:
  application:
    name: @artifactId@

# 注册中心
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://admin:123456@jud-cloud-eureka:9000/eureka/


